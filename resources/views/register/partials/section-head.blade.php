<div class="bns-admission-form__section-head">
    <span class="bns-admission-form__section-num" aria-hidden="true">{{ $num }}</span>
    <div>
        <h3 class="bns-admission-form__section-title">{{ $title }}</h3>
        @if(!empty($subtitle))
            <p class="bns-admission-form__section-sub">{{ $subtitle }}</p>
        @endif
    </div>
</div>
